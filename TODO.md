# TODO

- [x] Make `rfcview:rfc-link-button` work with `RET` key
- [x] handle `[RFC1234]` type notation in reader as a button
- [x] Handle Roman, Alphabet, even no preceding character type section index
- [ ] Make TOC entry jump to the section
- [x] Fix erasing first line just after ^L
